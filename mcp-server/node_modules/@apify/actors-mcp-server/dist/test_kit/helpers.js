
!function(){try{var e="undefined"!=typeof window?window:"undefined"!=typeof global?global:"undefined"!=typeof globalThis?globalThis:"undefined"!=typeof self?self:{},n=(new e.Error).stack;n&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[n]="cc54d505-fcdd-5599-b61c-39a93def6838")}catch(e){}}();
import Ajv from 'ajv';
import { expect, vi } from 'vitest';
import { actorNameToToolName, defaults, getCategoryTools, getDefaultTools, getExpectedToolNamesByCategories, } from '@apify/actors-mcp-server/internals.js';
import { APIFY_ACTOR_RUN_META_KEY, AUTO_INJECTED_TOOLS, HELPER_TOOLS, toolCategoriesEnabledByDefault, } from '@apify/actors-mcp-server/internals/test-kit.js';
// Live fixtures from apify/mcp-server-test-actor (see DEVELOPMENT.md).
export const ACTOR_NORMAL_MODE = 'apify/normal-mode-test-actor';
export const ACTOR_EXAMPLE_MCP_SERVER = 'apify/example-mcp-server';
const STDIO_TRANSPORT = 'stdio';
export const RETIRED_SELECTORS = ['add-actor', 'experimental', 'preview'];
export const AUTO_INJECTED_TOOL_NAMES = AUTO_INJECTED_TOOLS.map((t) => t.name);
export const DEFAULT_ACTOR_NAMES = defaults.actors.map((actor) => actorNameToToolName(actor));
// Lazy: avoids circular import during module init.
export function getDefaultToolNames() {
    return getExpectedToolNamesByCategories(toolCategoriesEnabledByDefault);
}
// report-problem is telemetry-gated (dev category); unit tests cover serve/hide/ack.
export function servedDefaultTools() {
    return getDefaultTools('default');
}
export function servedDefaultToolNames() {
    return getDefaultToolNames();
}
/** Create client, run testFn, always close. */
export function withClient(clientOptions, testFn) {
    return async (ctx) => {
        const client = await ctx.createClientFn(clientOptions);
        try {
            await testFn(client, ctx);
        }
        finally {
            await client.close();
        }
    };
}
/** Skip unless transport is legacy streamable HTTP (`2025-11-25`). */
export function skipUnlessLegacyHttp(ctx) {
    return ctx.transport !== '2025-11-25';
}
/** Skip on stdio. */
export function skipOnStdio(ctx) {
    return ctx.transport === STDIO_TRANSPORT;
}
/** Skip on every transport except stdio. */
export function skipUnlessStdio(ctx) {
    return ctx.transport !== STDIO_TRANSPORT;
}
export function getToolNames(tools) {
    return tools.tools.map((tool) => tool.name);
}
export function expectToolNamesToContain(names, toolNames = []) {
    toolNames.forEach((name) => expect(names).toContain(name));
}
export function buildExampleMcpServerAddToolContent(firstNumber, secondNumber) {
    return [
        {
            type: 'text',
            text: `The sum of ${firstNumber} and ${secondNumber} is ${firstNumber + secondNumber}`,
        },
    ];
}
export function validateStructuredOutput(result, toolOutputSchema, toolName) {
    const resultWithStructured = result;
    if (!resultWithStructured.structuredContent)
        return;
    const { structuredContent } = resultWithStructured;
    expect(toolOutputSchema).toBeDefined();
    if (toolOutputSchema) {
        const ajv = new Ajv();
        const validate = ajv.compile(toolOutputSchema);
        const isValid = validate(structuredContent);
        if (!isValid) {
            // eslint-disable-next-line no-console
            console.error(`Validation errors for ${toolName}:`, validate.errors);
        }
        expect(isValid).toBe(true);
        expect(validate.errors).toBeNull();
    }
}
/** Find tool by name for the given mode. */
export function findToolByName(name, mode) {
    const resolved = getCategoryTools(mode);
    for (const tools of Object.values(resolved)) {
        const tool = tools.find((t) => t.name === name);
        if (tool)
            return tool;
    }
    return undefined;
}
export function validateStructuredOutputForTool(result, toolName, mode) {
    var _a;
    validateStructuredOutput(result, (_a = findToolByName(toolName, mode)) === null || _a === void 0 ? void 0 : _a.outputSchema, toolName);
}
/** Assert non-empty readme + inputSchema; optional actorInfo.fullName. */
export function expectReadmeInStructuredContent(result, expectedActorFullName) {
    var _a, _b, _c, _d, _e;
    const r = result;
    expect(r.structuredContent).toBeDefined();
    if (expectedActorFullName) {
        expect((_b = (_a = r.structuredContent) === null || _a === void 0 ? void 0 : _a.actorInfo) === null || _b === void 0 ? void 0 : _b.fullName).toBe(expectedActorFullName);
    }
    expect((_c = r.structuredContent) === null || _c === void 0 ? void 0 : _c.readme).toBeDefined();
    expect(typeof ((_d = r.structuredContent) === null || _d === void 0 ? void 0 : _d.readme)).toBe('string');
    expect(r.structuredContent.readme.length).toBeGreaterThan(0);
    expect((_e = r.structuredContent) === null || _e === void 0 ? void 0 : _e.inputSchema).toBeDefined();
}
/** Assert apps-mode widget tools carry MCP Apps `_meta.ui` (SEP-1865). */
export function expectWidgetToolMeta(tools) {
    var _a;
    const toolNames = [
        HELPER_TOOLS.STORE_SEARCH_WIDGET,
        HELPER_TOOLS.ACTOR_GET_DETAILS_WIDGET,
        HELPER_TOOLS.ACTOR_CALL_WIDGET,
        HELPER_TOOLS.ACTOR_RUNS_GET_WIDGET,
    ];
    for (const toolName of toolNames) {
        const tool = tools.tools.find((t) => t.name === toolName);
        expect(tool).toBeDefined();
        expect(tool === null || tool === void 0 ? void 0 : tool._meta).toBeDefined();
        const ui = (_a = tool === null || tool === void 0 ? void 0 : tool._meta) === null || _a === void 0 ? void 0 : _a.ui;
        expect(ui).toBeDefined();
        expect(ui === null || ui === void 0 ? void 0 : ui.resourceUri).toBeDefined();
        expect(ui === null || ui === void 0 ? void 0 : ui.visibility).toEqual(['model', 'app']);
    }
}
/**
 * Assert canonical normal-mode-test-actor run response.
 * Skips `itemCount` (dataset metadata can lag); requires dataset id + fields.
 */
export function expectNormalModeTestStructuredContent(result) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q;
    const resultWithStructured = result;
    const sc = resultWithStructured.structuredContent;
    expect(sc).toBeDefined();
    expect(sc === null || sc === void 0 ? void 0 : sc.runId).toBeDefined();
    expect(sc === null || sc === void 0 ? void 0 : sc.status).toBe('SUCCEEDED');
    expect((_c = (_b = (_a = sc === null || sc === void 0 ? void 0 : sc.storages) === null || _a === void 0 ? void 0 : _a.datasets) === null || _b === void 0 ? void 0 : _b.default) === null || _c === void 0 ? void 0 : _c.id).toBeDefined();
    expect((_g = (_f = (_e = (_d = sc === null || sc === void 0 ? void 0 : sc.storages) === null || _d === void 0 ? void 0 : _d.datasets) === null || _e === void 0 ? void 0 : _e.default) === null || _f === void 0 ? void 0 : _f.fields) !== null && _g !== void 0 ? _g : []).toEqual(expect.arrayContaining(['firstNumber', 'secondNumber', 'sum']));
    expect(sc === null || sc === void 0 ? void 0 : sc.summary).toBeDefined();
    expect(sc === null || sc === void 0 ? void 0 : sc.nextStep).toBeDefined();
    // API-token clients get no Console URLs; UI-token path is unit-tested.
    expect(sc === null || sc === void 0 ? void 0 : sc.apifyConsoleUrl).toBeUndefined();
    expect((_k = (_j = (_h = sc === null || sc === void 0 ? void 0 : sc.storages) === null || _h === void 0 ? void 0 : _h.datasets) === null || _j === void 0 ? void 0 : _j.default) === null || _k === void 0 ? void 0 : _k.apifyConsoleUrl).toBeUndefined();
    expect((_o = (_m = (_l = sc === null || sc === void 0 ? void 0 : sc.storages) === null || _l === void 0 ? void 0 : _l.keyValueStores) === null || _m === void 0 ? void 0 : _m.default) === null || _o === void 0 ? void 0 : _o.apifyConsoleUrl).toBeUndefined();
    const narrative = (_q = (_p = resultWithStructured.content) === null || _p === void 0 ? void 0 : _p.map((c) => { var _a; return (_a = c.text) !== null && _a !== void 0 ? _a : ''; }).join('\n')) !== null && _q !== void 0 ? _q : '';
    expect(narrative).not.toContain('Apify Console:');
}
/** Assert Apify usage-cost `_meta` shape. */
export function expectUsageCostMeta(result) {
    var _a;
    const resultWithMeta = result;
    expect(resultWithMeta._meta).toBeDefined();
    const actorRun = (_a = resultWithMeta._meta) === null || _a === void 0 ? void 0 : _a['com.apify/ActorRun'];
    expect(actorRun).toBeDefined();
    expect(typeof (actorRun === null || actorRun === void 0 ? void 0 : actorRun.usageTotalUsd)).toBe('number');
    expect(actorRun.usageTotalUsd).toBeGreaterThanOrEqual(0);
    const usageUsd = actorRun === null || actorRun === void 0 ? void 0 : actorRun.usageUsd;
    if (usageUsd !== undefined) {
        expect(typeof usageUsd).toBe('object');
    }
}
const RUN_ABORT_WAIT_TIMEOUT_MS = 60000;
const RUN_ABORT_WAIT_INTERVAL_MS = 500;
const RUN_ID_PROGRESS_TIMEOUT_MS = 10000;
/** Capture runId from the first notifications/progress message. */
export function captureRunIdFromProgress() {
    let resolveRunId;
    const captured = new Promise((resolve) => {
        resolveRunId = resolve;
    });
    const onprogress = (progress) => {
        var _a;
        // Progress type omits _meta; present at runtime.
        const meta = progress._meta;
        const runId = (_a = meta === null || meta === void 0 ? void 0 : meta[APIFY_ACTOR_RUN_META_KEY]) === null || _a === void 0 ? void 0 : _a.runId;
        if (runId)
            resolveRunId(runId);
    };
    const runIdPromise = Promise.race([
        captured,
        new Promise((_, reject) => {
            const timer = setTimeout(() => reject(new Error(`No runId observed via notifications/progress within ${RUN_ID_PROGRESS_TIMEOUT_MS}ms`)), RUN_ID_PROGRESS_TIMEOUT_MS);
            timer.unref();
            void captured.then(() => clearTimeout(timer));
        }),
    ]);
    return { onprogress, runIdPromise };
}
/** Poll a specific run by ID until it reaches ABORTED or ABORTING. */
export async function waitForRunAborted(apiClient, runId) {
    await vi.waitUntil(async () => {
        const run = await apiClient.run(runId).get();
        return (run === null || run === void 0 ? void 0 : run.status) === 'ABORTED' || (run === null || run === void 0 ? void 0 : run.status) === 'ABORTING';
    }, { timeout: RUN_ABORT_WAIT_TIMEOUT_MS, interval: RUN_ABORT_WAIT_INTERVAL_MS });
}
export async function assertStatusMessagePropagated(taskClient, stream) {
    var _a;
    let taskId = null;
    let getTaskSawStatusMessage = false;
    let listTasksSawStatusMessage = false;
    for await (const message of stream) {
        if (message.type === 'taskCreated') {
            taskId = message.task.taskId;
        }
        else if (message.type === 'taskStatus') {
            if ((_a = message.task) === null || _a === void 0 ? void 0 : _a.statusMessage) {
                getTaskSawStatusMessage = true;
                if (!listTasksSawStatusMessage && taskId) {
                    const currentTaskId = taskId;
                    const tasksList = await taskClient.experimental.tasks.listTasks();
                    const currentTask = tasksList.tasks.find((task) => task.taskId === currentTaskId);
                    if (currentTask === null || currentTask === void 0 ? void 0 : currentTask.statusMessage)
                        listTasksSawStatusMessage = true;
                }
            }
        }
        else if (message.type === 'error') {
            throw message.error;
        }
    }
    expect(getTaskSawStatusMessage).toBe(true);
    expect(listTasksSawStatusMessage).toBe(true);
}
//# sourceMappingURL=helpers.js.map
//# debugId=cc54d505-fcdd-5599-b61c-39a93def6838
