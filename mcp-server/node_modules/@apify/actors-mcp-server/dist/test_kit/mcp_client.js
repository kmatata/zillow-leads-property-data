
!function(){try{var e="undefined"!=typeof window?window:"undefined"!=typeof global?global:"undefined"!=typeof globalThis?globalThis:"undefined"!=typeof self?self:{},n=(new e.Error).stack;n&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[n]="dfbf96d1-200e-5a89-bdca-b5485cc322df")}catch(e){}}();
import { Client as StatelessClient, StreamableHTTPClientTransport as StatelessStreamableHTTPClientTransport, } from '@modelcontextprotocol/client';
import { Client } from '@modelcontextprotocol/sdk/client/index.js';
import { StreamableHTTPClientTransport } from '@modelcontextprotocol/sdk/client/streamableHttp.js';
export function resolveToken(options) {
    if ((options === null || options === void 0 ? void 0 : options.token) === null)
        return undefined;
    if ((options === null || options === void 0 ? void 0 : options.token) !== undefined)
        return options.token;
    return process.env.APIFY_TOKEN;
}
/** Require a token unless `token: null` or payment mode. */
export function checkToken(options) {
    if (options === null || options === void 0 ? void 0 : options.payment)
        return;
    if ((options === null || options === void 0 ? void 0 : options.token) === null)
        return;
    if (!resolveToken(options)) {
        throw new Error('No token available: pass `token`, or set APIFY_TOKEN.');
    }
}
function buildAuthHeaders(options) {
    if (options === null || options === void 0 ? void 0 : options.payment)
        return {};
    const token = resolveToken(options);
    return token ? { authorization: `Bearer ${token}` } : {};
}
function appendSearchParams(url, options) {
    var _a;
    const { actors, tools, telemetry, serverMode, payment } = options !== null && options !== void 0 ? options : {};
    if (actors !== undefined)
        url.searchParams.append('actors', actors.join(','));
    if (tools !== undefined)
        url.searchParams.append('tools', tools.join(','));
    // Default to false for tests when not explicitly set.
    url.searchParams.append('telemetry-enabled', ((_a = telemetry === null || telemetry === void 0 ? void 0 : telemetry.enabled) !== null && _a !== void 0 ? _a : false).toString());
    if (serverMode !== undefined)
        url.searchParams.append('ui', serverMode);
    if (payment)
        url.searchParams.append('payment', payment);
}
export async function createMcpStreamableClient(serverUrl, options) {
    checkToken(options);
    const url = new URL(serverUrl);
    appendSearchParams(url, options);
    const transport = new StreamableHTTPClientTransport(url, {
        requestInit: { headers: buildAuthHeaders(options) },
    });
    const client = new Client({ name: (options === null || options === void 0 ? void 0 : options.clientName) || 'streamable-http-client', version: '1.0.0' });
    if (options === null || options === void 0 ? void 0 : options.clientCapabilities)
        client.registerCapabilities(options.clientCapabilities);
    await client.connect(transport);
    return client;
}
/**
 * v2 SDK client (2026-07-28). `versionNegotiation: auto` probes via `server/discover`.
 * Callers that must not fall back to legacy check `getDiscoverResult()`.
 */
export async function createMcpStatelessClient(serverUrl, options) {
    checkToken(options);
    const url = new URL(serverUrl);
    appendSearchParams(url, options);
    const transport = new StatelessStreamableHTTPClientTransport(url, {
        requestInit: { headers: buildAuthHeaders(options) },
    });
    const client = new StatelessClient({ name: (options === null || options === void 0 ? void 0 : options.clientName) || 'stateless-http-client', version: '1.0.0' }, { versionNegotiation: { mode: 'auto' } });
    if (options === null || options === void 0 ? void 0 : options.clientCapabilities) {
        // Shared option shape is v1; cast for v2 schema.
        client.registerCapabilities(options.clientCapabilities);
    }
    await client.connect(transport);
    return client;
}
//# sourceMappingURL=mcp_client.js.map
//# debugId=dfbf96d1-200e-5a89-bdca-b5485cc322df
