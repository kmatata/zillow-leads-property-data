import type { Case, CaseCtx } from './types.js';
/**
 * Register cases under `describe(suiteName)`.
 * - `isDeploymentTestOnly`: non-deployment-test → `it.skip`
 * - `skipIf`: also → `it.skip`
 * - `getFixture`: memoized once per call by `fixture.key`
 */
export declare function registerCases(suiteName: string, cases: Case[], ctx: Omit<CaseCtx, 'getFixture'>): void;
//# sourceMappingURL=register.d.ts.map