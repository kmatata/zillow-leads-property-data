import { Client as StatelessClient } from '@modelcontextprotocol/client';
import { Client } from '@modelcontextprotocol/sdk/client/index.js';
import type { ClientCapabilities } from '@modelcontextprotocol/sdk/types.js';
import type { ToolCategory } from '@apify/actors-mcp-server/internals.js';
import type { TelemetryEnv } from '@apify/actors-mcp-server/internals/test-kit.js';
/** Options for the published test-kit client factories. */
export interface SuiteClientOptions {
    actors?: string[];
    tools?: (ToolCategory | string)[];
    useEnv?: boolean;
    clientName?: string;
    telemetry?: {
        enabled?: boolean;
        env?: TelemetryEnv;
    };
    serverMode?: string;
    payment?: string;
    clientCapabilities?: ClientCapabilities;
    /** Bearer token. Omitted → `APIFY_TOKEN`. `null` → no Authorization header. */
    token?: string | null;
}
export declare function resolveToken(options?: SuiteClientOptions): string | undefined;
/** Require a token unless `token: null` or payment mode. */
export declare function checkToken(options?: SuiteClientOptions): void;
export declare function createMcpStreamableClient(serverUrl: string, options?: SuiteClientOptions): Promise<Client>;
/**
 * v2 SDK client (2026-07-28). `versionNegotiation: auto` probes via `server/discover`.
 * Callers that must not fall back to legacy check `getDiscoverResult()`.
 */
export declare function createMcpStatelessClient(serverUrl: string, options?: SuiteClientOptions): Promise<StatelessClient>;
//# sourceMappingURL=mcp_client.d.ts.map