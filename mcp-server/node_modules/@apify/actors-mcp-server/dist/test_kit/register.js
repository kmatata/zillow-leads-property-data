
!function(){try{var e="undefined"!=typeof window?window:"undefined"!=typeof global?global:"undefined"!=typeof globalThis?globalThis:"undefined"!=typeof self?self:{},n=(new e.Error).stack;n&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[n]="6e71ab19-ab2e-5c23-9a99-927fec0d499b")}catch(e){}}();
import { describe, it } from 'vitest';
/**
 * Register cases under `describe(suiteName)`.
 * - `isDeploymentTestOnly`: non-deployment-test → `it.skip`
 * - `skipIf`: also → `it.skip`
 * - `getFixture`: memoized once per call by `fixture.key`
 */
export function registerCases(suiteName, cases, ctx) {
    // eslint-disable-next-line vitest/valid-title -- suiteName is the caller's title string
    describe(suiteName, () => {
        var _a, _b;
        const fixtureCache = new Map();
        const fullCtx = {
            ...ctx,
            getFixture: async (fixture) => {
                let cached = fixtureCache.get(fixture.key);
                if (!cached) {
                    cached = fixture.setup(fullCtx);
                    fixtureCache.set(fixture.key, cached);
                }
                return cached;
            },
        };
        for (const c of cases) {
            const skip = (ctx.isDeploymentTestOnly && !c.isDeploymentTest) || ((_b = (_a = c.skipIf) === null || _a === void 0 ? void 0 : _a.call(c, fullCtx)) !== null && _b !== void 0 ? _b : false);
            const runIt = skip ? it.skip : it;
            const runFn = async () => c.run(fullCtx);
            if (c.retry !== undefined) {
                // eslint-disable-next-line vitest/valid-title vitest/expect-expect
                runIt(c.name, { retry: c.retry }, runFn);
            }
            else {
                // eslint-disable-next-line vitest/valid-title vitest/expect-expect
                runIt(c.name, runFn);
            }
        }
    });
}
//# sourceMappingURL=register.js.map
//# debugId=6e71ab19-ab2e-5c23-9a99-927fec0d499b
