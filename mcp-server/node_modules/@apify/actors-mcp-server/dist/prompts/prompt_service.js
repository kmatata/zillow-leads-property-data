
!function(){try{var e="undefined"!=typeof window?window:"undefined"!=typeof global?global:"undefined"!=typeof globalThis?globalThis:"undefined"!=typeof self?self:{},n=(new e.Error).stack;n&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[n]="76d575dc-2660-5fe4-be06-e0e0a33d71a0")}catch(e){}}();
import { InvalidParamsError } from '../mcp/errors.js';
/**
 * Prompt list/get logic, mirroring `createResourceService`'s factory + explicit-dependency shape.
 * Throws {@link InvalidParamsError} for an unknown prompt name or arguments that fail AJV validation;
 * the `server.ts` boundary maps that to the v1 InvalidParams JSON-RPC error at the wire seam.
 */
export function createPromptService(prompts) {
    const listPrompts = () => {
        return { prompts };
    };
    const getPrompt = (name, args) => {
        const prompt = prompts.find((p) => p.name === name);
        if (!prompt) {
            throw new InvalidParamsError(`Prompt ${name} not found. Available prompts: ${prompts.map((p) => p.name).join(', ')}`);
        }
        if (!prompt.ajvValidate(args)) {
            throw new InvalidParamsError(`Invalid arguments for prompt ${name}: args: ${JSON.stringify(args)} error: ${JSON.stringify(prompt.ajvValidate.errors)}`);
        }
        return {
            description: prompt.description,
            messages: [
                {
                    role: 'user',
                    content: {
                        type: 'text',
                        text: prompt.render(args || {}),
                    },
                },
            ],
        };
    };
    return {
        listPrompts,
        getPrompt,
    };
}
//# sourceMappingURL=prompt_service.js.map
//# debugId=76d575dc-2660-5fe4-be06-e0e0a33d71a0
