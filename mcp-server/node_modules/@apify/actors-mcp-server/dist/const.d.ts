export declare const ACTOR_README_MAX_LENGTH = 5000;
export declare const ACTOR_ENUM_MAX_LENGTH = 2000;
export declare const ACTOR_MAX_DESCRIPTION_LENGTH = 500;
export declare const ACTOR_MAX_MEMORY_MBYTES = 4096;
export declare const CODE_RUNTIME_ACTOR_NAME = "apify/code-runtime";
/**
 * Content larger than this is linked out instead of inlined, since inlining it would blow up the context
 * window (base64 inflates a binary payload ~33%, and a large text/JSON body overflows it just as easily).
 * The key-value-store-record tool caps binaries here (link to a fetchable URL); the API-resource proxy
 * caps every body here — its download is also aborted mid-flight at this limit via axios `maxContentLength`.
 */
export declare const MAX_INLINE_BYTES: number;
/**
 * Advisory threshold (uncompressed bytes) above which dataset tools append a size hint steering the
 * caller to narrow the fetch. A soft hint, not a truncation cap; ~50 KB mirrors the ~25k-token budget.
 */
export declare const DATASET_SIZE_HINT_BYTES = 50000;
/** Shared steer appended to large-output hints so the model narrows instead of refetching everything. */
export declare const NARROW_OUTPUT_HINT = "narrow with fields= or page with offset";
/** When `false`, `resolveServerMode('auto', ...)` forces {@link SERVER_MODE.DEFAULT} regardless of client capabilities. */
export declare const SERVER_MODE_AUTO_DETECTION_ENABLED = true;
export declare const SERVER_NAME = "apify-mcp-server";
export declare const SERVER_TITLE = "Apify MCP Server";
export declare const HELPER_TOOLS: {
    readonly ACTOR_CALL: "call-actor";
    readonly ACTOR_CALL_WIDGET: "call-actor-widget";
    readonly ACTOR_GET_DETAILS: "fetch-actor-details";
    readonly ACTOR_GET_DETAILS_WIDGET: "fetch-actor-details-widget";
    readonly ACTOR_RUNS_ABORT: "abort-actor-run";
    readonly ACTOR_RUNS_GET: "get-actor-run";
    readonly ACTOR_RUNS_GET_WIDGET: "get-actor-run-widget";
    readonly ACTOR_RUNS_LOG: "get-actor-log";
    readonly ACTOR_RUN_LIST_GET: "get-actor-run-list";
    readonly ACTOR_TASK_GET: "get-actor-task";
    readonly ACTOR_TASK_CREATE: "create-actor-task";
    readonly ACTOR_TASK_UPDATE: "update-actor-task";
    readonly ACTOR_TASK_PUBLISH: "publish-actor-task";
    readonly ACTOR_TASK_UNPUBLISH: "unpublish-actor-task";
    readonly DATASET_GET: "get-dataset";
    readonly DATASET_LIST_GET: "get-dataset-list";
    readonly DATASET_GET_ITEMS: "get-dataset-items";
    readonly DATASET_SCHEMA_GET: "get-dataset-schema";
    readonly KEY_VALUE_STORE_LIST_GET: "get-key-value-store-list";
    readonly KEY_VALUE_STORE_GET: "get-key-value-store";
    readonly KEY_VALUE_STORE_KEYS_GET: "get-key-value-store-keys";
    readonly KEY_VALUE_STORE_RECORD_GET: "get-key-value-store-record";
    readonly STORE_SEARCH: "search-actors";
    readonly STORE_SEARCH_WIDGET: "search-actors-widget";
    readonly DOCS_SEARCH: "search-apify-docs";
    readonly DOCS_FETCH: "fetch-apify-docs";
    readonly PROBLEM_REPORT: "report-problem";
};
export type HelperToolName = (typeof HELPER_TOOLS)[keyof typeof HELPER_TOOLS];
/**
 * Retired tool selectors: `add-actor` and `experimental` (add-actor was deleted in the stateless
 * migration) and the deprecated `preview` pseudo-category. They name neither a registry category
 * nor a real tool anymore, so they resolve to nothing — never loaded, never treated as an Actor ID,
 * never requiring a token by themselves.
 */
export declare const RETIRED_SELECTOR_NAMES: ReadonlySet<string>;
/**
 * Client-name substrings (lowercased, matched against `clientInfo.name`) that `report-problem` is
 * hidden from: Anthropic surfaces (Claude.ai / Claude Desktop / Claude Code /
 * `local-agent-mode-apify`) pending the directory review. Applied in the compose step — once per
 * connection on the 2025 path, per request on the 2026-07-28 one. Stateless `client-info` is
 * optional; a request declaring no client name matches no blocked substring and is served the tool
 * by policy. Substring matching covers new client builds without a maintained allowlist;
 * over-matching only hides an optional tool.
 */
export declare const REPORT_PROBLEM_BLOCKED_CLIENTS: string[];
/**
 * `clientInfo.name` sent by the Apify Console AI chat backend during the MCP initialize handshake.
 * Runs started by this client are attributed to the APIFY_AI request origin.
 */
export declare const APIFY_AI_CLIENT_NAME = "apify-console-ai-chat";
export declare const RAG_WEB_BROWSER = "apify/rag-web-browser";
export declare const RAG_WEB_BROWSER_WHITELISTED_FIELDS: string[];
export declare const RAG_WEB_BROWSER_ADDITIONAL_DESC = "Use this tool when user wants to GET or RETRIEVE actual data immediately (one-time data retrieval).\nThis tool scrapes the data itself - it does NOT just find tools.\n\nExamples of when to use:\n- User wants current/immediate data (e.g., \"Get flight prices for tomorrow\", \"What's the weather today?\")\n- User needs to fetch specific content now (e.g., \"Fetch news articles from CNN\", \"Get product info from Amazon\")\n- User has time indicators like \"today\", \"current\", \"latest\", \"recent\", \"now\"\n\nThis is for general web scraping and immediate data needs. For repeated/scheduled scraping of specific platforms (e-commerce, social media), consider suggesting a specialized Actor from the Store for better performance and reliability.";
export declare const defaults: {
    actors: string[];
};
/** API rejects `includeInputSchema=true` above this; mirrors apify-core `MAX_LIMIT_WITH_INPUT_SCHEMA`. */
export declare const MAX_LIMIT_WITH_INPUT_SCHEMA = 10;
/** Max input fields shown inline in text and structured Actor cards. */
export declare const MAX_INPUT_FIELDS_IN_ACTOR_CARD = 20;
export declare const ACTOR_PRICING_MODEL: {
    /** Rental Actors */
    readonly FLAT_PRICE_PER_MONTH: "FLAT_PRICE_PER_MONTH";
    readonly FREE: "FREE";
    /** Pay per result (PPR) Actors */
    readonly PRICE_PER_DATASET_ITEM: "PRICE_PER_DATASET_ITEM";
    /** Pay per event (PPE) Actors */
    readonly PAY_PER_EVENT: "PAY_PER_EVENT";
};
export declare const DOCS_SOURCES: readonly [{
    readonly id: "apify";
    readonly label: "Apify";
    readonly appId: "N8EOCSBQGH";
    readonly apiKey: "e97714a64e2b4b8b8fe0b01cd8592870";
    readonly indexName: "test_test_apify_sdk";
    readonly filters: "version:latest";
    readonly description: string;
}, {
    readonly id: "crawlee-js";
    readonly label: "Crawlee (JavaScript)";
    readonly appId: "5JC94MPMLY";
    readonly apiKey: "267679200b833c2ca1255ab276731869";
    readonly indexName: "crawlee";
    readonly typeFilter: "lvl1";
    readonly facetFilters: readonly ["language:en", readonly ["docusaurus_tag:default", "docusaurus_tag:docs-default-3.15"]];
    readonly description: string;
}, {
    readonly id: "crawlee-py";
    readonly label: "Crawlee (Python)";
    readonly appId: "5JC94MPMLY";
    readonly apiKey: "878493fcd7001e3c179b6db6796a999b";
    readonly indexName: "crawlee_python";
    readonly typeFilter: "lvl1";
    readonly facetFilters: readonly ["language:en", readonly ["docusaurus_tag:docs-default-current"]];
    readonly description: string;
}];
/**
 * Word window for Algolia `attributesToSnippet` in `search-apify-docs`. Bounds each hit to a
 * match-centered snippet instead of the full indexed `content` attribute — API-reference records
 * inline the whole OpenAPI schema (~34.5k chars each), so 20 hits returned ~173k tokens. The agent
 * fetches full pages via `fetch-apify-docs`.
 */
export declare const DOCS_SNIPPET_MAX_WORDS = 100;
/**
 * Sentinel used as Algolia `highlightPreTag`/`highlightPostTag` for docs snippets, stripped before
 * returning. An empty-string tag is treated as "unset" and falls back to Algolia's default
 * `<span class="algolia-docsearch-suggestion--highlight">` markup, so we set a private-use
 * character (U+E000) and remove it.
 */
export declare const DOCS_SNIPPET_HIGHLIGHT_TAG = "\uE000";
export declare const ALLOWED_DOC_DOMAINS: readonly ["https://docs.apify.com", "https://crawlee.dev"];
/** Actor usernames treated as official Apify (drives `isOfficialApify` on the Actor card). */
export declare const OFFICIAL_APIFY_USERNAMES: ReadonlySet<string>;
export declare const APIFY_STORE_URL = "https://apify.com";
/** Apify Console origin (production). */
export declare const CONSOLE_BASE_URL = "https://console.apify.com";
/** Apify Console origin on the staging cluster, selected when running on the staging MCP host. */
export declare const CONSOLE_BASE_URL_STAGING = "https://console-securitybyobscurity.apify.com";
/** Staging MCP host; mirrors the check in `getActorMCPServerURL` to pick staging vs production origins. */
export declare const STAGING_MCP_HOSTNAME = "mcp-securitybyobscurity.apify.com";
export declare const APIFY_FAVICON_URL = "https://apify.com/favicon.ico";
export declare const APIFY_LOGO_URL = "https://apify.com/apple-icon.png";
export declare const APIFY_MCP_URL = "https://mcp.apify.com";
export declare const APIFY_DOCS_MCP_URL = "https://docs.apify.com/platform/integrations/mcp";
export declare const TELEMETRY_ENV: {
    readonly DEV: "DEV";
    readonly PROD: "PROD";
};
export declare const DEFAULT_TELEMETRY_ENABLED = true;
export declare const DEFAULT_TELEMETRY_ENV: "PROD";
/**
 * Unified status constants for the tool execution lifecycle.
 * Single source of truth for all tool status values.
 */
export declare const TOOL_STATUS: {
    readonly SUCCEEDED: "SUCCEEDED";
    readonly FAILED: "FAILED";
    readonly ABORTED: "ABORTED";
    readonly SOFT_FAIL: "SOFT_FAIL";
};
export declare const FAILURE_CATEGORY: {
    readonly INVALID_INPUT: "INVALID_INPUT";
    readonly AUTH: "AUTH";
    readonly PERMISSION_APPROVAL_REQUIRED: "PERMISSION_APPROVAL_REQUIRED";
    readonly INTERNAL_ERROR: "INTERNAL_ERROR";
};
export declare const APIFY_ERROR_TYPE_FULL_PERMISSION_NOT_APPROVED = "full-permission-actor-not-approved";
export declare const APIFY_ERROR_TYPE_MEMORY_LIMIT_EXCEEDED = "memory-limit-exceeded";
export declare const APIFY_ERROR_TYPE_CANNOT_START_ACTOR_RUNS = "cannot-start-actor-runs";
export declare const APIFY_ERROR_TYPE_CANNOT_PUBLISH_ACTOR_TASK = "cannot-publish-actor-task";
export declare const HTTP_UNAUTHORIZED = 401;
export declare const HTTP_PAYMENT_REQUIRED = 402;
export declare const HTTP_FORBIDDEN = 403;
export declare const HTTP_NOT_FOUND = 404;
export declare const ALLOWED_TASK_TOOL_EXECUTION_MODES: readonly ["optional", "required"];
//# sourceMappingURL=const.d.ts.map