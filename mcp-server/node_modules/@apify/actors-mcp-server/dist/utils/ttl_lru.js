
!function(){try{var e="undefined"!=typeof window?window:"undefined"!=typeof global?global:"undefined"!=typeof globalThis?globalThis:"undefined"!=typeof self?self:{},n=(new e.Error).stack;n&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[n]="b2cce002-5516-524f-bc58-9b60227b70b2")}catch(e){}}();
import { LruCache } from '@apify/datastructures';
/**
 * LRU cache with TTL: entries expire after a set time and are checked (and evicted if expired)
 * whenever accessed.
 *
 * Usage:
 *   ```typescript
 *   const cache = new TTLLRUCache<string>(100, 60); // 100 items, 60 seconds TTL
 *   cache.set('key', 'value');
 *   const value = cache.get('key');
 *   ```
 */
export class TTLLRUCache {
    /**
     * @param maxLength Maximum number of items in the cache (LRU eviction)
     * @param ttlSecs Time-to-live for each entry, in seconds
     */
    constructor(maxLength, ttlSecs) {
        Object.defineProperty(this, "cache", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "ttlMillis", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.ttlMillis = ttlSecs * 1000;
        this.cache = new LruCache({
            maxLength,
        });
    }
    /**
     * Set a value in the cache with the given key. If the key exists, it is updated and TTL is reset.
     * @param key Cache key
     * @param value Value to store
     */
    set(key, value) {
        if (this.cache.get(key)) {
            this.cache.remove(key);
        }
        this.cache.add(key, {
            value,
            expiresAt: Date.now() + this.ttlMillis,
        });
    }
    /**
     * Get a value from the cache by key. Returns null if not found or expired.
     * @param key Cache key
     * @returns The value if present and not expired, otherwise null
     */
    get(key) {
        const entry = this.cache.get(key);
        if (entry && entry.expiresAt > Date.now()) {
            return entry.value;
        }
        this.cache.remove(key);
        return null;
    }
}
//# sourceMappingURL=ttl_lru.js.map
//# debugId=b2cce002-5516-524f-bc58-9b60227b70b2
