
!function(){try{var e="undefined"!=typeof window?window:"undefined"!=typeof global?global:"undefined"!=typeof globalThis?globalThis:"undefined"!=typeof self?self:{},n=(new e.Error).stack;n&&(e._sentryDebugIds=e._sentryDebugIds||{},e._sentryDebugIds[n]="e867c565-421e-5b0f-bfaa-4198a6c905fa")}catch(e){}}();
import { z } from 'zod';
import { toIsoString } from '../actors/actor_run_response.js';
const PUBLIC_CONFIG_FIELDS = [
    'seoTitle',
    'seoDescription',
    'inputSchemaFields',
    'datasetName',
    'datasetView',
];
/** Apify resource IDs are exactly 17 alphanumeric characters. */
const APIFY_ID_REGEX = /^[a-zA-Z0-9]{17}$/;
/**
 * The API reads an unqualified id as an ID, so a bare resource name has to be prefixed with `~` to be
 * resolved against the authenticated user's own resources. Ids that already carry a username, in either
 * the `username/name` or `username~name` format, and ids that are already IDs, are returned unchanged.
 *
 * Applies to both tasks and the Actor a task is created for, which the API resolves the same way.
 */
export function toSafeResourceId(idOrName) {
    const trimmed = idOrName.trim();
    if (trimmed.includes('/') || trimmed.includes('~'))
        return trimmed;
    return APIFY_ID_REGEX.test(trimmed) ? trimmed : `~${trimmed}`;
}
/** A bare 17-alnum value is shape-identical to an ID and to a legal task name. */
export function isAmbiguousResourceId(idOrName) {
    const trimmed = idOrName.trim();
    return !trimmed.includes('/') && !trimmed.includes('~') && APIFY_ID_REGEX.test(trimmed);
}
/**
 * Fetches a task by ID, name, or `username/name`. An ambiguous value cannot be resolved by
 * inspection, so it is read as an ID first and, on a miss, retried as the caller's own task name.
 */
export async function getTaskByIdOrName(client, idOrName) {
    const task = await client.task(toSafeResourceId(idOrName)).get();
    if (task || !isAmbiguousResourceId(idOrName))
        return task;
    return client.task(`~${idOrName.trim()}`).get();
}
/**
 * Task names must be DNS-safe and 3-63 characters, as the API enforces. Validated here so the
 * caller gets a usable message instead of a 400.
 */
const TASK_NAME_REGEX = /^([a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])$/;
export const taskNameSchema = z
    .string()
    .min(3)
    .max(63)
    .regex(TASK_NAME_REGEX, 'Task name may contain only letters, digits and dashes, and cannot start or end with a dash');
/** Writable public display configuration, shared by the create and update tools. */
export const publicConfigSchema = z.object({
    seoTitle: z.string().optional().describe('Title shown on the public landing page and in search results.'),
    seoDescription: z.string().optional().describe('Description shown on the public landing page.'),
    inputSchemaFields: z
        .array(z.string())
        .optional()
        .describe('Names of the input fields to display on the public page. At least one valid field is required to publish; the names must exist in the task input.'),
    datasetName: z.string().optional().describe('Name of the dataset whose schema provides the views.'),
    datasetView: z
        .string()
        .optional()
        .describe("View key from the Actor's dataset schema. Required to publish; ask the user, no tool lists dataset views."),
});
/**
 * The task subset returned by every task tool: identity, publication state, and the display
 * config. Input *values* are deliberately omitted — they may hold secrets — but the field names
 * are included because `publicConfig.inputSchemaFields` must reference them.
 */
export function taskResult(task) {
    var _a, _b, _c, _d;
    const publicConfig = task.publicConfig
        ? Object.fromEntries(PUBLIC_CONFIG_FIELDS.filter((field) => { var _a; return ((_a = task.publicConfig) === null || _a === void 0 ? void 0 : _a[field]) !== undefined; }).map((field) => {
            var _a;
            return [
                field,
                (_a = task.publicConfig) === null || _a === void 0 ? void 0 : _a[field],
            ];
        }))
        : null;
    return {
        taskId: task.id,
        actorId: task.actId,
        name: task.name,
        title: (_a = task.title) !== null && _a !== void 0 ? _a : null,
        description: (_b = task.description) !== null && _b !== void 0 ? _b : null,
        // Normalized because the client parses this into a `Date` while the raw publication call
        // returns a string; the declared output schema promises a string either way.
        publishedAt: (_d = toIsoString((_c = task.publicConfig) === null || _c === void 0 ? void 0 : _c.publishedAt)) !== null && _d !== void 0 ? _d : null,
        publicConfig,
        inputFields: task.input && !Array.isArray(task.input) ? Object.keys(task.input) : [],
    };
}
/**
 * Setting the publication state the task already has is a no-op, so both directions are safe
 * to repeat.
 */
export async function setTaskPublication(client, taskId, isPublic) {
    var _a;
    // An ambiguous value is resolved to the real ID by lookup; on a miss it falls through to the
    // ID reading so the publish call raises the API's own not-found error.
    const resolvedId = isAmbiguousResourceId(taskId) ? (_a = (await getTaskByIdOrName(client, taskId))) === null || _a === void 0 ? void 0 : _a.id : undefined;
    const taskClient = client.task(resolvedId !== null && resolvedId !== void 0 ? resolvedId : toSafeResourceId(taskId));
    return isPublic ? taskClient.publish() : taskClient.unpublish();
}
//# sourceMappingURL=task_helpers.js.map
//# debugId=e867c565-421e-5b0f-bfaa-4198a6c905fa
