import { z } from 'zod';
import type { ConsoleLinkContext, ToolEntry } from '../../types.js';
import { type ToolResponse } from '../../utils/mcp.js';
import { type FetchActorRunResult } from '../actors/actor_run_response.js';
/** Default `waitSecs` for `get-actor-run`. Intentionally non-zero so polling callers wait briefly by default. */
export declare const WAIT_SECS_DEFAULT = 30;
/**
 * Zod schema for `get-actor-run` arguments — shared between default and widget variants.
 */
export declare const getActorRunArgs: z.ZodObject<{
    runId: z.ZodString;
    waitSecs: z.ZodDefault<z.ZodOptional<z.ZodNumber>>;
}, z.core.$strip>;
export declare function buildGetActorRunError(runId: string, error: unknown): ToolResponse;
/**
 * Build the success response. `content[0]` is the JSON-stringified `structuredContent`
 * mirror (per MCP spec); `content[1]` carries an LLM-readable narrative of `summary` + `nextStep`.
 */
export declare function buildGetActorRunResponse(params: FetchActorRunResult & {
    linkContext?: ConsoleLinkContext;
}): ToolResponse;
/**
 * Build the widget success response. `content[1]` carries a short pointer instead of the
 * summary/nextStep narrative. Used only by `*-widget` tools; does not apply console links.
 */
export declare function buildGetActorRunWidgetResponse(params: FetchActorRunResult): ToolResponse;
/**
 * Default mode `get-actor-run` — returns without any widget metadata.
 */
export declare const getActorRun: ToolEntry;
//# sourceMappingURL=get_actor_run.d.ts.map