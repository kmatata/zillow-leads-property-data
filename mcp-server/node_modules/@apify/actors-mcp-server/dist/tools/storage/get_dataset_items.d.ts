import type { ToolEntry } from '../../types.js';
export declare const DEFAULT_DATASET_ITEMS_LIMIT = 20;
/** Top-level dot prefixes of `fields`. Apify's `flatten` recurses, so the first segment suffices. */
export declare function extractDotPrefixes(fields: string[]): string[];
/**
 * https://docs.apify.com/api/v2/dataset-items-get
 */
export declare const getDatasetItems: ToolEntry;
//# sourceMappingURL=get_dataset_items.d.ts.map